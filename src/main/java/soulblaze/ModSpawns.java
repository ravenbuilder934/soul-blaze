package soulblaze;

/*
import net.minecraft.entity.EntityClassification;
import net.minecraft.world.biome.Biome;
import net.minecraft.world.biome.MobSpawnInfo.Spawners;
import net.minecraftforge.event.world.BiomeLoadingEvent;
import net.minecraftforge.eventbus.api.EventPriority;
import net.minecraftforge.eventbus.api.SubscribeEvent;
import net.minecraftforge.fml.common.Mod;

@Mod.EventBusSubscriber(modid = SoulBlaze.MOD_ID)
public class ModSpawns
{
	@SubscribeEvent(priority = EventPriority.HIGH)
	public static void onBiomeLoading(BiomeLoadingEvent event)
	{
		if (event.getCategory() != Biome.Category.OCEAN && event.getCategory() != Biome.Category.RIVER)
		{
			if (Config.SOUL_BLAZE_SPAWN_PROBABILITY.get() > 0 && Config.SOUL_BLAZE_MIN_SPAWN_SIZE.get() > 0 && Config.SOUL_BLAZE_MAX_SPAWN_SIZE.get() > 0)
				event.getSpawns().getSpawner(EntityClassification.MONSTER).add(new Spawners(ModEntities.SOUL_BLAZE, Config.SOUL_BLAZE_SPAWN_PROBABILITY.get(), Config.SOUL_BLAZE_MIN_SPAWN_SIZE.get(), Config.SOUL_BLAZE_MAX_SPAWN_SIZE.get()));
		}
	}
}
*/
